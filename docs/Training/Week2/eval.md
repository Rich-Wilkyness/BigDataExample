- Dimensional Modeling = use of star and snowflake
- Entity-Relationship Modeling = based on the needs. Does not follow star or snowflake method
- Inmon Modeling = older model. 

- data vault = 
- data mesh = 

- what is a dataframe?
    - is structured data, (sql is also)
        - has schema, rows, and columns

- what is structured vs semi-structured vs unstructured
    - semi = json or yaml, usually json -> allows you to have extra key pairs that not all rows (individual data) follows
        - semi also has nested values 
    - unst = plain text

- know these databases:
    - oracle database services
    - mariaDB, postgreSQL, mySQL

- crontab uses cronjob (it's just one tool in the belt)


- what is the purpose of docker

- what is ci cd

