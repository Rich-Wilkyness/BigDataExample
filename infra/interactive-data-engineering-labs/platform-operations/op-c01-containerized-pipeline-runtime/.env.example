# run_pipeline.sh copies this file to the ignored .env file on first use.
POSTGRES_USER=pipeline
POSTGRES_PASSWORD=op-c01-local-password
POSTGRES_DB=op_c01
# Host port only; containers communicate with postgres:5432.
POSTGRES_PORT=55432
