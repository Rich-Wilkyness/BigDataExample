# SQL workspace

Versioned SQL belongs here when curriculum examples are implemented. Separate
schema definitions, transformations, analytical queries, and verification when
that distinction makes ownership or execution order clearer.

Queries should state their dialect and execution engine. Important behavior
must be covered by executable tests or recorded query-plan evidence rather than
being verified only through manual copy and paste.

